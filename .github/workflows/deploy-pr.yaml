name: deploy-ebf
on:
  pull_request:
    branches:
      - 'ebf_*'
    types:
      - opened
      - synchronize
jobs:
  build-and-deploy-ebf:
    name: Build and deploy ebf
    runs-on: ubuntu-latest
    steps:
        - run: echo "job triggered by ${{ github.event_name }} ${{ github.base_ref }} event"
   
